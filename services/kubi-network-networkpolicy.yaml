apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: kubi-network
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/kubi-network: "true"
  podSelector:
    matchLabels:
      io.kompose.network/kubi-network: "true"
